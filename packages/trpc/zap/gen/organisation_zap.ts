// Code generated by zapgen --target=ts; DO NOT EDIT.
// source: organisation-router.zap

import {
  Builder,
  Message,
  StructView,
} from "@hanzo/zap";

const organisationRouterStubOff = {
  Router: 0,
} as const;
const organisationRouterStubSize = 8;

/** Zero-copy view into a ZAP-encoded OrganisationRouterStub message. */
export class OrganisationRouterStub extends StructView {
  /** Parse `b` and return a typed view. Throws on a wire-level check failure (magic, version, size). */
  static wrap(b: Uint8Array): OrganisationRouterStub {
    const root = Message.parse(b).root();
    return new OrganisationRouterStub(root.data, root.offset);
  }

  get router(): string { return this.text(organisationRouterStubOff.Router); }
}

/** Field values for newOrganisationRouterStub. */
export interface OrganisationRouterStubInput {
  router: string;
}

/** Build a ZAP-encoded OrganisationRouterStub message from `in` and return the bytes. */
export function newOrganisationRouterStub(in_: OrganisationRouterStubInput): Uint8Array {
  const b = new Builder(256);
  const ob = b.startObject(organisationRouterStubSize);
  ob.setText(organisationRouterStubOff.Router, in_.router);
  ob.finishAsRoot();
  return b.finish();
}


