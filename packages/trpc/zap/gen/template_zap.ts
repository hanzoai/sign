// Code generated by zapgen --target=ts; DO NOT EDIT.
// source: template-router.zap

import {
  Builder,
  Message,
  StructView,
} from "@hanzo/zap";

const templateRouterStubOff = {
  Router: 0,
} as const;
const templateRouterStubSize = 8;

/** Zero-copy view into a ZAP-encoded TemplateRouterStub message. */
export class TemplateRouterStub extends StructView {
  /** Parse `b` and return a typed view. Throws on a wire-level check failure (magic, version, size). */
  static wrap(b: Uint8Array): TemplateRouterStub {
    const root = Message.parse(b).root();
    return new TemplateRouterStub(root.data, root.offset);
  }

  get router(): string { return this.text(templateRouterStubOff.Router); }
}

/** Field values for newTemplateRouterStub. */
export interface TemplateRouterStubInput {
  router: string;
}

/** Build a ZAP-encoded TemplateRouterStub message from `in` and return the bytes. */
export function newTemplateRouterStub(in_: TemplateRouterStubInput): Uint8Array {
  const b = new Builder(256);
  const ob = b.startObject(templateRouterStubSize);
  ob.setText(templateRouterStubOff.Router, in_.router);
  ob.finishAsRoot();
  return b.finish();
}


