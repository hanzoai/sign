// Code generated by zapgen --target=ts; DO NOT EDIT.
// source: embedding-router.zap

import {
  Builder,
  Message,
  StructView,
} from "@hanzo/zap";

const embeddingRouterStubOff = {
  Router: 0,
} as const;
const embeddingRouterStubSize = 8;

/** Zero-copy view into a ZAP-encoded EmbeddingRouterStub message. */
export class EmbeddingRouterStub extends StructView {
  /** Parse `b` and return a typed view. Throws on a wire-level check failure (magic, version, size). */
  static wrap(b: Uint8Array): EmbeddingRouterStub {
    const root = Message.parse(b).root();
    return new EmbeddingRouterStub(root.data, root.offset);
  }

  get router(): string { return this.text(embeddingRouterStubOff.Router); }
}

/** Field values for newEmbeddingRouterStub. */
export interface EmbeddingRouterStubInput {
  router: string;
}

/** Build a ZAP-encoded EmbeddingRouterStub message from `in` and return the bytes. */
export function newEmbeddingRouterStub(in_: EmbeddingRouterStubInput): Uint8Array {
  const b = new Builder(256);
  const ob = b.startObject(embeddingRouterStubSize);
  ob.setText(embeddingRouterStubOff.Router, in_.router);
  ob.finishAsRoot();
  return b.finish();
}


