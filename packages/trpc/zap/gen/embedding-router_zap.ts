// Code generated by zapgen --target=ts; DO NOT EDIT.
// source: embedding-router.zap

import {
  Builder,
  Message,
  StructView,
  buildRequest,
  parseRequest,
  buildResponse,
  parseResponse,
  NO_TARGET,
  Status,
  type Call,
  type Response,
} from "@zap-proto/zap";

const reqOff = {
  Body: 0,
} as const;
const reqSize = 8;

/** Zero-copy view into a ZAP-encoded Req message. */
export class Req extends StructView {
  /** Parse `b` and return a typed view. Throws on a wire-level check failure (magic, version, size). */
  static wrap(b: Uint8Array): Req {
    const root = Message.parse(b).root();
    return new Req(root.data, root.offset);
  }

  get body(): string { return this.text(reqOff.Body); }
}

/** Field values for newReq. */
export interface ReqInput {
  body: string;
}

/** Build a ZAP-encoded Req message from `in` and return the bytes. */
export function newReq(in_: ReqInput): Uint8Array {
  const b = new Builder(256);
  const ob = b.startObject(reqSize);
  ob.setText(reqOff.Body, in_.body);
  ob.finishAsRoot();
  return b.finish();
}


const respOff = {
  Body: 0,
} as const;
const respSize = 8;

/** Zero-copy view into a ZAP-encoded Resp message. */
export class Resp extends StructView {
  /** Parse `b` and return a typed view. Throws on a wire-level check failure (magic, version, size). */
  static wrap(b: Uint8Array): Resp {
    const root = Message.parse(b).root();
    return new Resp(root.data, root.offset);
  }

  get body(): string { return this.text(respOff.Body); }
}

/** Field values for newResp. */
export interface RespInput {
  body: string;
}

/** Build a ZAP-encoded Resp message from `in` and return the bytes. */
export function newResp(in_: RespInput): Uint8Array {
  const b = new Builder(256);
  const ob = b.startObject(respSize);
  ob.setText(respOff.Body, in_.body);
  ob.finishAsRoot();
  return b.finish();
}


/** Method ordinals for the EmbeddingPresign service (stable wire ids). */
export const EmbeddingPresignMethod = {
  createEmbeddingPresignToken: 1,
  verifyEmbeddingPresignToken: 2,
  createEmbeddingEnvelope: 3,
  createEmbeddingDocument: 4,
  createEmbeddingTemplate: 5,
  updateEmbeddingEnvelope: 6,
  updateEmbeddingDocument: 7,
  updateEmbeddingTemplate: 8,
  getMultiSignDocument: 9,
} as const;

/** Typed RPC client for the EmbeddingPresign service over a ZAP call channel. */
export interface EmbeddingPresignChannel {
  /** Ship one Call envelope and await its correlated Response. */
  call(envelope: Uint8Array): Promise<Response>;
}

export class EmbeddingPresignClient {
  constructor(private readonly channel: EmbeddingPresignChannel, private readonly cap: Uint8Array = new Uint8Array(0)) {}

  async createEmbeddingPresignToken(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: EmbeddingPresignMethod.createEmbeddingPresignToken,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`EmbeddingPresign.createEmbeddingPresignToken: status ${resp.status}`);
    }
    return resp.body;
  }

  async verifyEmbeddingPresignToken(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: EmbeddingPresignMethod.verifyEmbeddingPresignToken,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`EmbeddingPresign.verifyEmbeddingPresignToken: status ${resp.status}`);
    }
    return resp.body;
  }

  async createEmbeddingEnvelope(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: EmbeddingPresignMethod.createEmbeddingEnvelope,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`EmbeddingPresign.createEmbeddingEnvelope: status ${resp.status}`);
    }
    return resp.body;
  }

  async createEmbeddingDocument(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: EmbeddingPresignMethod.createEmbeddingDocument,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`EmbeddingPresign.createEmbeddingDocument: status ${resp.status}`);
    }
    return resp.body;
  }

  async createEmbeddingTemplate(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: EmbeddingPresignMethod.createEmbeddingTemplate,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`EmbeddingPresign.createEmbeddingTemplate: status ${resp.status}`);
    }
    return resp.body;
  }

  async updateEmbeddingEnvelope(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: EmbeddingPresignMethod.updateEmbeddingEnvelope,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`EmbeddingPresign.updateEmbeddingEnvelope: status ${resp.status}`);
    }
    return resp.body;
  }

  async updateEmbeddingDocument(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: EmbeddingPresignMethod.updateEmbeddingDocument,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`EmbeddingPresign.updateEmbeddingDocument: status ${resp.status}`);
    }
    return resp.body;
  }

  async updateEmbeddingTemplate(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: EmbeddingPresignMethod.updateEmbeddingTemplate,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`EmbeddingPresign.updateEmbeddingTemplate: status ${resp.status}`);
    }
    return resp.body;
  }

  async getMultiSignDocument(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: EmbeddingPresignMethod.getMultiSignDocument,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`EmbeddingPresign.getMultiSignDocument: status ${resp.status}`);
    }
    return resp.body;
  }

}

/** Abstract RPC server for the EmbeddingPresign service. Subclass and implement each method. */
export abstract class EmbeddingPresignServer {
  abstract createEmbeddingPresignToken(req: Uint8Array): Promise<Uint8Array>;
  abstract verifyEmbeddingPresignToken(req: Uint8Array): Promise<Uint8Array>;
  abstract createEmbeddingEnvelope(req: Uint8Array): Promise<Uint8Array>;
  abstract createEmbeddingDocument(req: Uint8Array): Promise<Uint8Array>;
  abstract createEmbeddingTemplate(req: Uint8Array): Promise<Uint8Array>;
  abstract updateEmbeddingEnvelope(req: Uint8Array): Promise<Uint8Array>;
  abstract updateEmbeddingDocument(req: Uint8Array): Promise<Uint8Array>;
  abstract updateEmbeddingTemplate(req: Uint8Array): Promise<Uint8Array>;
  abstract getMultiSignDocument(req: Uint8Array): Promise<Uint8Array>;

  /** Decode a Call envelope, route by method ordinal, and build the Response envelope. */
  async dispatch(envelope: Uint8Array): Promise<Uint8Array> {
    const call: Call = parseRequest(envelope);
    switch (call.method) {
      case EmbeddingPresignMethod.createEmbeddingPresignToken: {
        const body = await this.createEmbeddingPresignToken(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case EmbeddingPresignMethod.verifyEmbeddingPresignToken: {
        const body = await this.verifyEmbeddingPresignToken(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case EmbeddingPresignMethod.createEmbeddingEnvelope: {
        const body = await this.createEmbeddingEnvelope(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case EmbeddingPresignMethod.createEmbeddingDocument: {
        const body = await this.createEmbeddingDocument(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case EmbeddingPresignMethod.createEmbeddingTemplate: {
        const body = await this.createEmbeddingTemplate(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case EmbeddingPresignMethod.updateEmbeddingEnvelope: {
        const body = await this.updateEmbeddingEnvelope(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case EmbeddingPresignMethod.updateEmbeddingDocument: {
        const body = await this.updateEmbeddingDocument(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case EmbeddingPresignMethod.updateEmbeddingTemplate: {
        const body = await this.updateEmbeddingTemplate(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case EmbeddingPresignMethod.getMultiSignDocument: {
        const body = await this.getMultiSignDocument(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      default:
        return buildResponse(Status.NotFound, call.promiseID, new Uint8Array(0));
    }
  }
}


