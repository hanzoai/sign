// Code generated by zapgen --target=ts; DO NOT EDIT.
// source: admin-router.zap

import {
  Builder,
  Message,
  StructView,
} from "@hanzo/zap";

const adminRouterStubOff = {
  Router: 0,
} as const;
const adminRouterStubSize = 8;

/** Zero-copy view into a ZAP-encoded AdminRouterStub message. */
export class AdminRouterStub extends StructView {
  /** Parse `b` and return a typed view. Throws on a wire-level check failure (magic, version, size). */
  static wrap(b: Uint8Array): AdminRouterStub {
    const root = Message.parse(b).root();
    return new AdminRouterStub(root.data, root.offset);
  }

  get router(): string { return this.text(adminRouterStubOff.Router); }
}

/** Field values for newAdminRouterStub. */
export interface AdminRouterStubInput {
  router: string;
}

/** Build a ZAP-encoded AdminRouterStub message from `in` and return the bytes. */
export function newAdminRouterStub(in_: AdminRouterStubInput): Uint8Array {
  const b = new Builder(256);
  const ob = b.startObject(adminRouterStubSize);
  ob.setText(adminRouterStubOff.Router, in_.router);
  ob.finishAsRoot();
  return b.finish();
}


