// Code generated by zapgen --target=ts; DO NOT EDIT.
// source: profile-router.zap

import {
  Builder,
  Message,
  StructView,
  buildRequest,
  parseRequest,
  buildResponse,
  parseResponse,
  NO_TARGET,
  Status,
  type Call,
  type Response,
} from "@zap-proto/zap";

const reqOff = {
  Body: 0,
} as const;
const reqSize = 8;

/** Zero-copy view into a ZAP-encoded Req message. */
export class Req extends StructView {
  /** Parse `b` and return a typed view. Throws on a wire-level check failure (magic, version, size). */
  static wrap(b: Uint8Array): Req {
    const root = Message.parse(b).root();
    return new Req(root.data, root.offset);
  }

  get body(): string { return this.text(reqOff.Body); }
}

/** Field values for newReq. */
export interface ReqInput {
  body: string;
}

/** Build a ZAP-encoded Req message from `in` and return the bytes. */
export function newReq(in_: ReqInput): Uint8Array {
  const b = new Builder(256);
  const ob = b.startObject(reqSize);
  ob.setText(reqOff.Body, in_.body);
  ob.finishAsRoot();
  return b.finish();
}


const respOff = {
  Body: 0,
} as const;
const respSize = 8;

/** Zero-copy view into a ZAP-encoded Resp message. */
export class Resp extends StructView {
  /** Parse `b` and return a typed view. Throws on a wire-level check failure (magic, version, size). */
  static wrap(b: Uint8Array): Resp {
    const root = Message.parse(b).root();
    return new Resp(root.data, root.offset);
  }

  get body(): string { return this.text(respOff.Body); }
}

/** Field values for newResp. */
export interface RespInput {
  body: string;
}

/** Build a ZAP-encoded Resp message from `in` and return the bytes. */
export function newResp(in_: RespInput): Uint8Array {
  const b = new Builder(256);
  const ob = b.startObject(respSize);
  ob.setText(respOff.Body, in_.body);
  ob.finishAsRoot();
  return b.finish();
}


/** Method ordinals for the Profile service (stable wire ids). */
export const ProfileMethod = {
  findUserSecurityAuditLogs: 1,
  updateProfile: 2,
  deleteAccount: 3,
  setProfileImage: 4,
  submitSupportTicket: 5,
} as const;

/** Typed RPC client for the Profile service over a ZAP call channel. */
export interface ProfileChannel {
  /** Ship one Call envelope and await its correlated Response. */
  call(envelope: Uint8Array): Promise<Response>;
}

export class ProfileClient {
  constructor(private readonly channel: ProfileChannel, private readonly cap: Uint8Array = new Uint8Array(0)) {}

  async findUserSecurityAuditLogs(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: ProfileMethod.findUserSecurityAuditLogs,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Profile.findUserSecurityAuditLogs: status ${resp.status}`);
    }
    return resp.body;
  }

  async updateProfile(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: ProfileMethod.updateProfile,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Profile.updateProfile: status ${resp.status}`);
    }
    return resp.body;
  }

  async deleteAccount(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: ProfileMethod.deleteAccount,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Profile.deleteAccount: status ${resp.status}`);
    }
    return resp.body;
  }

  async setProfileImage(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: ProfileMethod.setProfileImage,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Profile.setProfileImage: status ${resp.status}`);
    }
    return resp.body;
  }

  async submitSupportTicket(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: ProfileMethod.submitSupportTicket,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Profile.submitSupportTicket: status ${resp.status}`);
    }
    return resp.body;
  }

}

/** Abstract RPC server for the Profile service. Subclass and implement each method. */
export abstract class ProfileServer {
  abstract findUserSecurityAuditLogs(req: Uint8Array): Promise<Uint8Array>;
  abstract updateProfile(req: Uint8Array): Promise<Uint8Array>;
  abstract deleteAccount(req: Uint8Array): Promise<Uint8Array>;
  abstract setProfileImage(req: Uint8Array): Promise<Uint8Array>;
  abstract submitSupportTicket(req: Uint8Array): Promise<Uint8Array>;

  /** Decode a Call envelope, route by method ordinal, and build the Response envelope. */
  async dispatch(envelope: Uint8Array): Promise<Uint8Array> {
    const call: Call = parseRequest(envelope);
    switch (call.method) {
      case ProfileMethod.findUserSecurityAuditLogs: {
        const body = await this.findUserSecurityAuditLogs(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case ProfileMethod.updateProfile: {
        const body = await this.updateProfile(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case ProfileMethod.deleteAccount: {
        const body = await this.deleteAccount(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case ProfileMethod.setProfileImage: {
        const body = await this.setProfileImage(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case ProfileMethod.submitSupportTicket: {
        const body = await this.submitSupportTicket(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      default:
        return buildResponse(Status.NotFound, call.promiseID, new Uint8Array(0));
    }
  }
}


