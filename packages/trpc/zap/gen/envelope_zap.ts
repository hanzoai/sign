// Code generated by zapgen --target=ts; DO NOT EDIT.
// source: envelope-router.zap

import {
  Builder,
  Message,
  StructView,
} from "@hanzo/zap";

const envelopeRouterStubOff = {
  Router: 0,
} as const;
const envelopeRouterStubSize = 8;

/** Zero-copy view into a ZAP-encoded EnvelopeRouterStub message. */
export class EnvelopeRouterStub extends StructView {
  /** Parse `b` and return a typed view. Throws on a wire-level check failure (magic, version, size). */
  static wrap(b: Uint8Array): EnvelopeRouterStub {
    const root = Message.parse(b).root();
    return new EnvelopeRouterStub(root.data, root.offset);
  }

  get router(): string { return this.text(envelopeRouterStubOff.Router); }
}

/** Field values for newEnvelopeRouterStub. */
export interface EnvelopeRouterStubInput {
  router: string;
}

/** Build a ZAP-encoded EnvelopeRouterStub message from `in` and return the bytes. */
export function newEnvelopeRouterStub(in_: EnvelopeRouterStubInput): Uint8Array {
  const b = new Builder(256);
  const ob = b.startObject(envelopeRouterStubSize);
  ob.setText(envelopeRouterStubOff.Router, in_.router);
  ob.finishAsRoot();
  return b.finish();
}


