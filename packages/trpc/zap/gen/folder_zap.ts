// Code generated by zapgen --target=ts; DO NOT EDIT.
// source: folder-router.zap

import {
  Builder,
  Message,
  StructView,
} from "@hanzo/zap";

const folderQueryOff = {
  Type: 0,
  ParentId: 8,
  Page: 16,
  PerPage: 20,
} as const;
const folderQuerySize = 24;

/** Zero-copy view into a ZAP-encoded FolderQuery message. */
export class FolderQuery extends StructView {
  /** Parse `b` and return a typed view. Throws on a wire-level check failure (magic, version, size). */
  static wrap(b: Uint8Array): FolderQuery {
    const root = Message.parse(b).root();
    return new FolderQuery(root.data, root.offset);
  }

  get type(): string { return this.text(folderQueryOff.Type); }
  get parentId(): string { return this.text(folderQueryOff.ParentId); }
  get page(): number { return this.u32(folderQueryOff.Page); }
  get perPage(): number { return this.u32(folderQueryOff.PerPage); }
}

/** Field values for newFolderQuery. */
export interface FolderQueryInput {
  type: string;
  parentId: string;
  page: number;
  perPage: number;
}

/** Build a ZAP-encoded FolderQuery message from `in` and return the bytes. */
export function newFolderQuery(in_: FolderQueryInput): Uint8Array {
  const b = new Builder(256);
  const ob = b.startObject(folderQuerySize);
  ob.setText(folderQueryOff.Type, in_.type);
  ob.setText(folderQueryOff.ParentId, in_.parentId);
  ob.setU32(folderQueryOff.Page, in_.page);
  ob.setU32(folderQueryOff.PerPage, in_.perPage);
  ob.finishAsRoot();
  return b.finish();
}


const createFolderInputOff = {
  Name: 0,
  Type: 8,
  ParentId: 16,
} as const;
const createFolderInputSize = 24;

/** Zero-copy view into a ZAP-encoded CreateFolderInput message. */
export class CreateFolderInput extends StructView {
  /** Parse `b` and return a typed view. Throws on a wire-level check failure (magic, version, size). */
  static wrap(b: Uint8Array): CreateFolderInput {
    const root = Message.parse(b).root();
    return new CreateFolderInput(root.data, root.offset);
  }

  get name(): string { return this.text(createFolderInputOff.Name); }
  get type(): string { return this.text(createFolderInputOff.Type); }
  get parentId(): string { return this.text(createFolderInputOff.ParentId); }
}

/** Field values for newCreateFolderInput. */
export interface CreateFolderInputInput {
  name: string;
  type: string;
  parentId: string;
}

/** Build a ZAP-encoded CreateFolderInput message from `in` and return the bytes. */
export function newCreateFolderInput(in_: CreateFolderInputInput): Uint8Array {
  const b = new Builder(256);
  const ob = b.startObject(createFolderInputSize);
  ob.setText(createFolderInputOff.Name, in_.name);
  ob.setText(createFolderInputOff.Type, in_.type);
  ob.setText(createFolderInputOff.ParentId, in_.parentId);
  ob.finishAsRoot();
  return b.finish();
}


const updateFolderInputOff = {
  Id: 0,
  Name: 8,
  Visibility: 16,
} as const;
const updateFolderInputSize = 24;

/** Zero-copy view into a ZAP-encoded UpdateFolderInput message. */
export class UpdateFolderInput extends StructView {
  /** Parse `b` and return a typed view. Throws on a wire-level check failure (magic, version, size). */
  static wrap(b: Uint8Array): UpdateFolderInput {
    const root = Message.parse(b).root();
    return new UpdateFolderInput(root.data, root.offset);
  }

  get id(): string { return this.text(updateFolderInputOff.Id); }
  get name(): string { return this.text(updateFolderInputOff.Name); }
  get visibility(): string { return this.text(updateFolderInputOff.Visibility); }
}

/** Field values for newUpdateFolderInput. */
export interface UpdateFolderInputInput {
  id: string;
  name: string;
  visibility: string;
}

/** Build a ZAP-encoded UpdateFolderInput message from `in` and return the bytes. */
export function newUpdateFolderInput(in_: UpdateFolderInputInput): Uint8Array {
  const b = new Builder(256);
  const ob = b.startObject(updateFolderInputSize);
  ob.setText(updateFolderInputOff.Id, in_.id);
  ob.setText(updateFolderInputOff.Name, in_.name);
  ob.setText(updateFolderInputOff.Visibility, in_.visibility);
  ob.finishAsRoot();
  return b.finish();
}


const deleteFolderInputOff = {
  Id: 0,
} as const;
const deleteFolderInputSize = 8;

/** Zero-copy view into a ZAP-encoded DeleteFolderInput message. */
export class DeleteFolderInput extends StructView {
  /** Parse `b` and return a typed view. Throws on a wire-level check failure (magic, version, size). */
  static wrap(b: Uint8Array): DeleteFolderInput {
    const root = Message.parse(b).root();
    return new DeleteFolderInput(root.data, root.offset);
  }

  get id(): string { return this.text(deleteFolderInputOff.Id); }
}

/** Field values for newDeleteFolderInput. */
export interface DeleteFolderInputInput {
  id: string;
}

/** Build a ZAP-encoded DeleteFolderInput message from `in` and return the bytes. */
export function newDeleteFolderInput(in_: DeleteFolderInputInput): Uint8Array {
  const b = new Builder(256);
  const ob = b.startObject(deleteFolderInputSize);
  ob.setText(deleteFolderInputOff.Id, in_.id);
  ob.finishAsRoot();
  return b.finish();
}


