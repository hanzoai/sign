// Code generated by zapgen --target=ts; DO NOT EDIT.
// source: profile-router.zap

import {
  Builder,
  Message,
  StructView,
} from "@hanzo/zap";

const auditLogQueryOff = {
  Page: 0,
  PerPage: 4,
} as const;
const auditLogQuerySize = 8;

/** Zero-copy view into a ZAP-encoded AuditLogQuery message. */
export class AuditLogQuery extends StructView {
  /** Parse `b` and return a typed view. Throws on a wire-level check failure (magic, version, size). */
  static wrap(b: Uint8Array): AuditLogQuery {
    const root = Message.parse(b).root();
    return new AuditLogQuery(root.data, root.offset);
  }

  get page(): number { return this.u32(auditLogQueryOff.Page); }
  get perPage(): number { return this.u32(auditLogQueryOff.PerPage); }
}

/** Field values for newAuditLogQuery. */
export interface AuditLogQueryInput {
  page: number;
  perPage: number;
}

/** Build a ZAP-encoded AuditLogQuery message from `in` and return the bytes. */
export function newAuditLogQuery(in_: AuditLogQueryInput): Uint8Array {
  const b = new Builder(256);
  const ob = b.startObject(auditLogQuerySize);
  ob.setU32(auditLogQueryOff.Page, in_.page);
  ob.setU32(auditLogQueryOff.PerPage, in_.perPage);
  ob.finishAsRoot();
  return b.finish();
}


const updateProfileInputOff = {
  Name: 0,
  Signature: 8,
} as const;
const updateProfileInputSize = 16;

/** Zero-copy view into a ZAP-encoded UpdateProfileInput message. */
export class UpdateProfileInput extends StructView {
  /** Parse `b` and return a typed view. Throws on a wire-level check failure (magic, version, size). */
  static wrap(b: Uint8Array): UpdateProfileInput {
    const root = Message.parse(b).root();
    return new UpdateProfileInput(root.data, root.offset);
  }

  get name(): string { return this.text(updateProfileInputOff.Name); }
  get signature(): string { return this.text(updateProfileInputOff.Signature); }
}

/** Field values for newUpdateProfileInput. */
export interface UpdateProfileInputInput {
  name: string;
  signature: string;
}

/** Build a ZAP-encoded UpdateProfileInput message from `in` and return the bytes. */
export function newUpdateProfileInput(in_: UpdateProfileInputInput): Uint8Array {
  const b = new Builder(256);
  const ob = b.startObject(updateProfileInputSize);
  ob.setText(updateProfileInputOff.Name, in_.name);
  ob.setText(updateProfileInputOff.Signature, in_.signature);
  ob.finishAsRoot();
  return b.finish();
}


const setProfileImageInputOff = {
  ImageData: 0,
  TeamId: 8,
  OrganisationId: 12,
} as const;
const setProfileImageInputSize = 20;

/** Zero-copy view into a ZAP-encoded SetProfileImageInput message. */
export class SetProfileImageInput extends StructView {
  /** Parse `b` and return a typed view. Throws on a wire-level check failure (magic, version, size). */
  static wrap(b: Uint8Array): SetProfileImageInput {
    const root = Message.parse(b).root();
    return new SetProfileImageInput(root.data, root.offset);
  }

  get imageData(): string { return this.text(setProfileImageInputOff.ImageData); }
  get teamId(): number { return this.u32(setProfileImageInputOff.TeamId); }
  get organisationId(): string { return this.text(setProfileImageInputOff.OrganisationId); }
}

/** Field values for newSetProfileImageInput. */
export interface SetProfileImageInputInput {
  imageData: string;
  teamId: number;
  organisationId: string;
}

/** Build a ZAP-encoded SetProfileImageInput message from `in` and return the bytes. */
export function newSetProfileImageInput(in_: SetProfileImageInputInput): Uint8Array {
  const b = new Builder(256);
  const ob = b.startObject(setProfileImageInputSize);
  ob.setText(setProfileImageInputOff.ImageData, in_.imageData);
  ob.setU32(setProfileImageInputOff.TeamId, in_.teamId);
  ob.setText(setProfileImageInputOff.OrganisationId, in_.organisationId);
  ob.finishAsRoot();
  return b.finish();
}


const supportTicketInputOff = {
  OrganisationId: 0,
  TeamId: 8,
  Subject: 16,
  Message: 24,
} as const;
const supportTicketInputSize = 32;

/** Zero-copy view into a ZAP-encoded SupportTicketInput message. */
export class SupportTicketInput extends StructView {
  /** Parse `b` and return a typed view. Throws on a wire-level check failure (magic, version, size). */
  static wrap(b: Uint8Array): SupportTicketInput {
    const root = Message.parse(b).root();
    return new SupportTicketInput(root.data, root.offset);
  }

  get organisationId(): string { return this.text(supportTicketInputOff.OrganisationId); }
  get teamId(): string { return this.text(supportTicketInputOff.TeamId); }
  get subject(): string { return this.text(supportTicketInputOff.Subject); }
  get message(): string { return this.text(supportTicketInputOff.Message); }
}

/** Field values for newSupportTicketInput. */
export interface SupportTicketInputInput {
  organisationId: string;
  teamId: string;
  subject: string;
  message: string;
}

/** Build a ZAP-encoded SupportTicketInput message from `in` and return the bytes. */
export function newSupportTicketInput(in_: SupportTicketInputInput): Uint8Array {
  const b = new Builder(256);
  const ob = b.startObject(supportTicketInputSize);
  ob.setText(supportTicketInputOff.OrganisationId, in_.organisationId);
  ob.setText(supportTicketInputOff.TeamId, in_.teamId);
  ob.setText(supportTicketInputOff.Subject, in_.subject);
  ob.setText(supportTicketInputOff.Message, in_.message);
  ob.finishAsRoot();
  return b.finish();
}


