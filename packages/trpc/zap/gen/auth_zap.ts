// Code generated by zapgen --target=ts; DO NOT EDIT.
// source: auth-router.zap

import {
  Builder,
  Message,
  StructView,
} from "@hanzo/zap";

const authRouterStubOff = {
  Router: 0,
} as const;
const authRouterStubSize = 8;

/** Zero-copy view into a ZAP-encoded AuthRouterStub message. */
export class AuthRouterStub extends StructView {
  /** Parse `b` and return a typed view. Throws on a wire-level check failure (magic, version, size). */
  static wrap(b: Uint8Array): AuthRouterStub {
    const root = Message.parse(b).root();
    return new AuthRouterStub(root.data, root.offset);
  }

  get router(): string { return this.text(authRouterStubOff.Router); }
}

/** Field values for newAuthRouterStub. */
export interface AuthRouterStubInput {
  router: string;
}

/** Build a ZAP-encoded AuthRouterStub message from `in` and return the bytes. */
export function newAuthRouterStub(in_: AuthRouterStubInput): Uint8Array {
  const b = new Builder(256);
  const ob = b.startObject(authRouterStubSize);
  ob.setText(authRouterStubOff.Router, in_.router);
  ob.finishAsRoot();
  return b.finish();
}


