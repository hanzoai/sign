// Code generated by zapgen --target=ts; DO NOT EDIT.
// source: team-router.zap

import {
  Builder,
  Message,
  StructView,
} from "@hanzo/zap";

const teamRouterStubOff = {
  Router: 0,
} as const;
const teamRouterStubSize = 8;

/** Zero-copy view into a ZAP-encoded TeamRouterStub message. */
export class TeamRouterStub extends StructView {
  /** Parse `b` and return a typed view. Throws on a wire-level check failure (magic, version, size). */
  static wrap(b: Uint8Array): TeamRouterStub {
    const root = Message.parse(b).root();
    return new TeamRouterStub(root.data, root.offset);
  }

  get router(): string { return this.text(teamRouterStubOff.Router); }
}

/** Field values for newTeamRouterStub. */
export interface TeamRouterStubInput {
  router: string;
}

/** Build a ZAP-encoded TeamRouterStub message from `in` and return the bytes. */
export function newTeamRouterStub(in_: TeamRouterStubInput): Uint8Array {
  const b = new Builder(256);
  const ob = b.startObject(teamRouterStubSize);
  ob.setText(teamRouterStubOff.Router, in_.router);
  ob.finishAsRoot();
  return b.finish();
}


