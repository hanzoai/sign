// Code generated by zapgen --target=ts; DO NOT EDIT.
// source: webhook-router.zap

import {
  Builder,
  Message,
  StructView,
} from "@hanzo/zap";

const webhookRouterStubOff = {
  Router: 0,
} as const;
const webhookRouterStubSize = 8;

/** Zero-copy view into a ZAP-encoded WebhookRouterStub message. */
export class WebhookRouterStub extends StructView {
  /** Parse `b` and return a typed view. Throws on a wire-level check failure (magic, version, size). */
  static wrap(b: Uint8Array): WebhookRouterStub {
    const root = Message.parse(b).root();
    return new WebhookRouterStub(root.data, root.offset);
  }

  get router(): string { return this.text(webhookRouterStubOff.Router); }
}

/** Field values for newWebhookRouterStub. */
export interface WebhookRouterStubInput {
  router: string;
}

/** Build a ZAP-encoded WebhookRouterStub message from `in` and return the bytes. */
export function newWebhookRouterStub(in_: WebhookRouterStubInput): Uint8Array {
  const b = new Builder(256);
  const ob = b.startObject(webhookRouterStubSize);
  ob.setText(webhookRouterStubOff.Router, in_.router);
  ob.finishAsRoot();
  return b.finish();
}


