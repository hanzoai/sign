// Code generated by zapgen --target=ts; DO NOT EDIT.
// source: document-router.zap

import {
  Builder,
  Message,
  StructView,
} from "@hanzo/zap";

const documentRouterStubOff = {
  Router: 0,
} as const;
const documentRouterStubSize = 8;

/** Zero-copy view into a ZAP-encoded DocumentRouterStub message. */
export class DocumentRouterStub extends StructView {
  /** Parse `b` and return a typed view. Throws on a wire-level check failure (magic, version, size). */
  static wrap(b: Uint8Array): DocumentRouterStub {
    const root = Message.parse(b).root();
    return new DocumentRouterStub(root.data, root.offset);
  }

  get router(): string { return this.text(documentRouterStubOff.Router); }
}

/** Field values for newDocumentRouterStub. */
export interface DocumentRouterStubInput {
  router: string;
}

/** Build a ZAP-encoded DocumentRouterStub message from `in` and return the bytes. */
export function newDocumentRouterStub(in_: DocumentRouterStubInput): Uint8Array {
  const b = new Builder(256);
  const ob = b.startObject(documentRouterStubSize);
  ob.setText(documentRouterStubOff.Router, in_.router);
  ob.finishAsRoot();
  return b.finish();
}


