// Code generated by zapgen --target=ts; DO NOT EDIT.
// source: recipient-router.zap

import {
  Builder,
  Message,
  StructView,
  buildRequest,
  parseRequest,
  buildResponse,
  NO_TARGET,
  Status,
  type Call,
  type Response,
} from "@zap-proto/zap";

const reqOff = {
  Body: 0,
} as const;
const reqSize = 8;

/** Zero-copy view into a ZAP-encoded Req message. */
export class Req extends StructView {
  /** Parse `b` and return a typed view. Throws on a wire-level check failure (magic, version, size). */
  static wrap(b: Uint8Array): Req {
    const root = Message.parse(b).root();
    return new Req(root.data, root.offset);
  }

  get body(): string { return this.text(reqOff.Body); }
}

/** Field values for newReq. */
export interface ReqInput {
  body: string;
}

/** Build a ZAP-encoded Req message from `in` and return the bytes. */
export function newReq(in_: ReqInput): Uint8Array {
  const b = new Builder(256);
  const ob = b.startObject(reqSize);
  ob.setText(reqOff.Body, in_.body);
  ob.finishAsRoot();
  return b.finish();
}


const respOff = {
  Body: 0,
} as const;
const respSize = 8;

/** Zero-copy view into a ZAP-encoded Resp message. */
export class Resp extends StructView {
  /** Parse `b` and return a typed view. Throws on a wire-level check failure (magic, version, size). */
  static wrap(b: Uint8Array): Resp {
    const root = Message.parse(b).root();
    return new Resp(root.data, root.offset);
  }

  get body(): string { return this.text(respOff.Body); }
}

/** Field values for newResp. */
export interface RespInput {
  body: string;
}

/** Build a ZAP-encoded Resp message from `in` and return the bytes. */
export function newResp(in_: RespInput): Uint8Array {
  const b = new Builder(256);
  const ob = b.startObject(respSize);
  ob.setText(respOff.Body, in_.body);
  ob.finishAsRoot();
  return b.finish();
}


/** Method ordinals for the Recipient service (stable wire ids). */
export const RecipientMethod = {
  suggestionsFind: 1,
  getDocumentRecipient: 2,
  createDocumentRecipient: 3,
  createDocumentRecipients: 4,
  updateDocumentRecipient: 5,
  updateDocumentRecipients: 6,
  deleteDocumentRecipient: 7,
  setDocumentRecipients: 8,
  getTemplateRecipient: 9,
  createTemplateRecipient: 10,
  createTemplateRecipients: 11,
  updateTemplateRecipient: 12,
  updateTemplateRecipients: 13,
  deleteTemplateRecipient: 14,
  setTemplateRecipients: 15,
  completeDocumentWithToken: 16,
  rejectDocumentWithToken: 17,
} as const;

/** Typed RPC client for the Recipient service over a ZAP call channel. */
export interface RecipientChannel {
  /** Ship one Call envelope and await its correlated Response. */
  call(envelope: Uint8Array): Promise<Response>;
}

export class RecipientClient {
  constructor(private readonly channel: RecipientChannel, private readonly cap: Uint8Array = new Uint8Array(0)) {}

  async suggestionsFind(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: RecipientMethod.suggestionsFind,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Recipient.suggestionsFind: status ${resp.status}`);
    }
    return resp.body;
  }

  async getDocumentRecipient(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: RecipientMethod.getDocumentRecipient,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Recipient.getDocumentRecipient: status ${resp.status}`);
    }
    return resp.body;
  }

  async createDocumentRecipient(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: RecipientMethod.createDocumentRecipient,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Recipient.createDocumentRecipient: status ${resp.status}`);
    }
    return resp.body;
  }

  async createDocumentRecipients(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: RecipientMethod.createDocumentRecipients,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Recipient.createDocumentRecipients: status ${resp.status}`);
    }
    return resp.body;
  }

  async updateDocumentRecipient(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: RecipientMethod.updateDocumentRecipient,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Recipient.updateDocumentRecipient: status ${resp.status}`);
    }
    return resp.body;
  }

  async updateDocumentRecipients(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: RecipientMethod.updateDocumentRecipients,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Recipient.updateDocumentRecipients: status ${resp.status}`);
    }
    return resp.body;
  }

  async deleteDocumentRecipient(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: RecipientMethod.deleteDocumentRecipient,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Recipient.deleteDocumentRecipient: status ${resp.status}`);
    }
    return resp.body;
  }

  async setDocumentRecipients(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: RecipientMethod.setDocumentRecipients,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Recipient.setDocumentRecipients: status ${resp.status}`);
    }
    return resp.body;
  }

  async getTemplateRecipient(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: RecipientMethod.getTemplateRecipient,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Recipient.getTemplateRecipient: status ${resp.status}`);
    }
    return resp.body;
  }

  async createTemplateRecipient(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: RecipientMethod.createTemplateRecipient,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Recipient.createTemplateRecipient: status ${resp.status}`);
    }
    return resp.body;
  }

  async createTemplateRecipients(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: RecipientMethod.createTemplateRecipients,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Recipient.createTemplateRecipients: status ${resp.status}`);
    }
    return resp.body;
  }

  async updateTemplateRecipient(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: RecipientMethod.updateTemplateRecipient,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Recipient.updateTemplateRecipient: status ${resp.status}`);
    }
    return resp.body;
  }

  async updateTemplateRecipients(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: RecipientMethod.updateTemplateRecipients,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Recipient.updateTemplateRecipients: status ${resp.status}`);
    }
    return resp.body;
  }

  async deleteTemplateRecipient(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: RecipientMethod.deleteTemplateRecipient,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Recipient.deleteTemplateRecipient: status ${resp.status}`);
    }
    return resp.body;
  }

  async setTemplateRecipients(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: RecipientMethod.setTemplateRecipients,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Recipient.setTemplateRecipients: status ${resp.status}`);
    }
    return resp.body;
  }

  async completeDocumentWithToken(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: RecipientMethod.completeDocumentWithToken,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Recipient.completeDocumentWithToken: status ${resp.status}`);
    }
    return resp.body;
  }

  async rejectDocumentWithToken(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: RecipientMethod.rejectDocumentWithToken,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Recipient.rejectDocumentWithToken: status ${resp.status}`);
    }
    return resp.body;
  }

}

/** Abstract RPC server for the Recipient service. Subclass and implement each method. */
export abstract class RecipientServer {
  abstract suggestionsFind(req: Uint8Array): Promise<Uint8Array>;
  abstract getDocumentRecipient(req: Uint8Array): Promise<Uint8Array>;
  abstract createDocumentRecipient(req: Uint8Array): Promise<Uint8Array>;
  abstract createDocumentRecipients(req: Uint8Array): Promise<Uint8Array>;
  abstract updateDocumentRecipient(req: Uint8Array): Promise<Uint8Array>;
  abstract updateDocumentRecipients(req: Uint8Array): Promise<Uint8Array>;
  abstract deleteDocumentRecipient(req: Uint8Array): Promise<Uint8Array>;
  abstract setDocumentRecipients(req: Uint8Array): Promise<Uint8Array>;
  abstract getTemplateRecipient(req: Uint8Array): Promise<Uint8Array>;
  abstract createTemplateRecipient(req: Uint8Array): Promise<Uint8Array>;
  abstract createTemplateRecipients(req: Uint8Array): Promise<Uint8Array>;
  abstract updateTemplateRecipient(req: Uint8Array): Promise<Uint8Array>;
  abstract updateTemplateRecipients(req: Uint8Array): Promise<Uint8Array>;
  abstract deleteTemplateRecipient(req: Uint8Array): Promise<Uint8Array>;
  abstract setTemplateRecipients(req: Uint8Array): Promise<Uint8Array>;
  abstract completeDocumentWithToken(req: Uint8Array): Promise<Uint8Array>;
  abstract rejectDocumentWithToken(req: Uint8Array): Promise<Uint8Array>;

  /** Decode a Call envelope, route by method ordinal, and build the Response envelope. */
  async dispatch(envelope: Uint8Array): Promise<Uint8Array> {
    const call: Call = parseRequest(envelope);
    switch (call.method) {
      case RecipientMethod.suggestionsFind: {
        const body = await this.suggestionsFind(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case RecipientMethod.getDocumentRecipient: {
        const body = await this.getDocumentRecipient(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case RecipientMethod.createDocumentRecipient: {
        const body = await this.createDocumentRecipient(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case RecipientMethod.createDocumentRecipients: {
        const body = await this.createDocumentRecipients(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case RecipientMethod.updateDocumentRecipient: {
        const body = await this.updateDocumentRecipient(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case RecipientMethod.updateDocumentRecipients: {
        const body = await this.updateDocumentRecipients(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case RecipientMethod.deleteDocumentRecipient: {
        const body = await this.deleteDocumentRecipient(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case RecipientMethod.setDocumentRecipients: {
        const body = await this.setDocumentRecipients(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case RecipientMethod.getTemplateRecipient: {
        const body = await this.getTemplateRecipient(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case RecipientMethod.createTemplateRecipient: {
        const body = await this.createTemplateRecipient(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case RecipientMethod.createTemplateRecipients: {
        const body = await this.createTemplateRecipients(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case RecipientMethod.updateTemplateRecipient: {
        const body = await this.updateTemplateRecipient(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case RecipientMethod.updateTemplateRecipients: {
        const body = await this.updateTemplateRecipients(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case RecipientMethod.deleteTemplateRecipient: {
        const body = await this.deleteTemplateRecipient(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case RecipientMethod.setTemplateRecipients: {
        const body = await this.setTemplateRecipients(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case RecipientMethod.completeDocumentWithToken: {
        const body = await this.completeDocumentWithToken(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case RecipientMethod.rejectDocumentWithToken: {
        const body = await this.rejectDocumentWithToken(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      default:
        return buildResponse(Status.NotFound, call.promiseID, new Uint8Array(0));
    }
  }
}


