// Code generated by zapgen --target=ts; DO NOT EDIT.
// source: transport.zap

import {
  Builder,
  Message,
  StructView,
} from "@zap-proto/zap";

const zapRequestOff = {
  Method: 0,
  Payload: 8,
  TeamId: 16,
} as const;
const zapRequestSize = 24;

/** Zero-copy view into a ZAP-encoded ZapRequest message. */
export class ZapRequest extends StructView {
  /** Parse `b` and return a typed view. Throws on a wire-level check failure (magic, version, size). */
  static wrap(b: Uint8Array): ZapRequest {
    const root = Message.parse(b).root();
    return new ZapRequest(root.data, root.offset);
  }

  get method(): string { return this.text(zapRequestOff.Method); }
  get payload(): string { return this.text(zapRequestOff.Payload); }
  get teamId(): string { return this.text(zapRequestOff.TeamId); }
}

/** Field values for newZapRequest. */
export interface ZapRequestInput {
  method: string;
  payload: string;
  teamId: string;
}

/** Build a ZAP-encoded ZapRequest message from `in` and return the bytes. */
export function newZapRequest(in_: ZapRequestInput): Uint8Array {
  const b = new Builder(256);
  const ob = b.startObject(zapRequestSize);
  ob.setText(zapRequestOff.Method, in_.method);
  ob.setText(zapRequestOff.Payload, in_.payload);
  ob.setText(zapRequestOff.TeamId, in_.teamId);
  ob.finishAsRoot();
  return b.finish();
}


const zapReplyOff = {
  Ok: 0,
  Status: 4,
  Result: 8,
  ErrorJson: 16,
} as const;
const zapReplySize = 24;

/** Zero-copy view into a ZAP-encoded ZapReply message. */
export class ZapReply extends StructView {
  /** Parse `b` and return a typed view. Throws on a wire-level check failure (magic, version, size). */
  static wrap(b: Uint8Array): ZapReply {
    const root = Message.parse(b).root();
    return new ZapReply(root.data, root.offset);
  }

  get ok(): boolean { return this.bool(zapReplyOff.Ok); }
  get status(): number { return this.u32(zapReplyOff.Status); }
  get result(): string { return this.text(zapReplyOff.Result); }
  get errorJson(): string { return this.text(zapReplyOff.ErrorJson); }
}

/** Field values for newZapReply. */
export interface ZapReplyInput {
  ok: boolean;
  status: number;
  result: string;
  errorJson: string;
}

/** Build a ZAP-encoded ZapReply message from `in` and return the bytes. */
export function newZapReply(in_: ZapReplyInput): Uint8Array {
  const b = new Builder(256);
  const ob = b.startObject(zapReplySize);
  ob.setBool(zapReplyOff.Ok, in_.ok);
  ob.setU32(zapReplyOff.Status, in_.status);
  ob.setText(zapReplyOff.Result, in_.result);
  ob.setText(zapReplyOff.ErrorJson, in_.errorJson);
  ob.finishAsRoot();
  return b.finish();
}


