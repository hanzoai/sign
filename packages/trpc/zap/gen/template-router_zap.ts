// Code generated by zapgen --target=ts; DO NOT EDIT.
// source: template-router.zap

import {
  Builder,
  Message,
  StructView,
  buildRequest,
  parseRequest,
  buildResponse,
  NO_TARGET,
  Status,
  type Call,
  type Response,
} from "@zap-proto/zap";

const reqOff = {
  Body: 0,
} as const;
const reqSize = 8;

/** Zero-copy view into a ZAP-encoded Req message. */
export class Req extends StructView {
  /** Parse `b` and return a typed view. Throws on a wire-level check failure (magic, version, size). */
  static wrap(b: Uint8Array): Req {
    const root = Message.parse(b).root();
    return new Req(root.data, root.offset);
  }

  get body(): string { return this.text(reqOff.Body); }
}

/** Field values for newReq. */
export interface ReqInput {
  body: string;
}

/** Build a ZAP-encoded Req message from `in` and return the bytes. */
export function newReq(in_: ReqInput): Uint8Array {
  const b = new Builder(256);
  const ob = b.startObject(reqSize);
  ob.setText(reqOff.Body, in_.body);
  ob.finishAsRoot();
  return b.finish();
}


const respOff = {
  Body: 0,
} as const;
const respSize = 8;

/** Zero-copy view into a ZAP-encoded Resp message. */
export class Resp extends StructView {
  /** Parse `b` and return a typed view. Throws on a wire-level check failure (magic, version, size). */
  static wrap(b: Uint8Array): Resp {
    const root = Message.parse(b).root();
    return new Resp(root.data, root.offset);
  }

  get body(): string { return this.text(respOff.Body); }
}

/** Field values for newResp. */
export interface RespInput {
  body: string;
}

/** Build a ZAP-encoded Resp message from `in` and return the bytes. */
export function newResp(in_: RespInput): Uint8Array {
  const b = new Builder(256);
  const ob = b.startObject(respSize);
  ob.setText(respOff.Body, in_.body);
  ob.finishAsRoot();
  return b.finish();
}


/** Method ordinals for the Template service (stable wire ids). */
export const TemplateMethod = {
  findTemplates: 1,
  getTemplateById: 2,
  getMany: 3,
  createTemplate: 4,
  createTemplateTemporary: 5,
  updateTemplate: 6,
  duplicateTemplate: 7,
  deleteTemplate: 8,
  createDocumentFromTemplate: 9,
  createDocumentFromDirectTemplate: 10,
  createTemplateDirectLink: 11,
  deleteTemplateDirectLink: 12,
  toggleTemplateDirectLink: 13,
  uploadBulkSend: 14,
} as const;

/** Typed RPC client for the Template service over a ZAP call channel. */
export interface TemplateChannel {
  /** Ship one Call envelope and await its correlated Response. */
  call(envelope: Uint8Array): Promise<Response>;
}

export class TemplateClient {
  constructor(private readonly channel: TemplateChannel, private readonly cap: Uint8Array = new Uint8Array(0)) {}

  async findTemplates(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: TemplateMethod.findTemplates,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Template.findTemplates: status ${resp.status}`);
    }
    return resp.body;
  }

  async getTemplateById(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: TemplateMethod.getTemplateById,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Template.getTemplateById: status ${resp.status}`);
    }
    return resp.body;
  }

  async getMany(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: TemplateMethod.getMany,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Template.getMany: status ${resp.status}`);
    }
    return resp.body;
  }

  async createTemplate(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: TemplateMethod.createTemplate,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Template.createTemplate: status ${resp.status}`);
    }
    return resp.body;
  }

  async createTemplateTemporary(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: TemplateMethod.createTemplateTemporary,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Template.createTemplateTemporary: status ${resp.status}`);
    }
    return resp.body;
  }

  async updateTemplate(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: TemplateMethod.updateTemplate,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Template.updateTemplate: status ${resp.status}`);
    }
    return resp.body;
  }

  async duplicateTemplate(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: TemplateMethod.duplicateTemplate,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Template.duplicateTemplate: status ${resp.status}`);
    }
    return resp.body;
  }

  async deleteTemplate(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: TemplateMethod.deleteTemplate,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Template.deleteTemplate: status ${resp.status}`);
    }
    return resp.body;
  }

  async createDocumentFromTemplate(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: TemplateMethod.createDocumentFromTemplate,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Template.createDocumentFromTemplate: status ${resp.status}`);
    }
    return resp.body;
  }

  async createDocumentFromDirectTemplate(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: TemplateMethod.createDocumentFromDirectTemplate,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Template.createDocumentFromDirectTemplate: status ${resp.status}`);
    }
    return resp.body;
  }

  async createTemplateDirectLink(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: TemplateMethod.createTemplateDirectLink,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Template.createTemplateDirectLink: status ${resp.status}`);
    }
    return resp.body;
  }

  async deleteTemplateDirectLink(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: TemplateMethod.deleteTemplateDirectLink,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Template.deleteTemplateDirectLink: status ${resp.status}`);
    }
    return resp.body;
  }

  async toggleTemplateDirectLink(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: TemplateMethod.toggleTemplateDirectLink,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Template.toggleTemplateDirectLink: status ${resp.status}`);
    }
    return resp.body;
  }

  async uploadBulkSend(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: TemplateMethod.uploadBulkSend,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Template.uploadBulkSend: status ${resp.status}`);
    }
    return resp.body;
  }

}

/** Abstract RPC server for the Template service. Subclass and implement each method. */
export abstract class TemplateServer {
  abstract findTemplates(req: Uint8Array): Promise<Uint8Array>;
  abstract getTemplateById(req: Uint8Array): Promise<Uint8Array>;
  abstract getMany(req: Uint8Array): Promise<Uint8Array>;
  abstract createTemplate(req: Uint8Array): Promise<Uint8Array>;
  abstract createTemplateTemporary(req: Uint8Array): Promise<Uint8Array>;
  abstract updateTemplate(req: Uint8Array): Promise<Uint8Array>;
  abstract duplicateTemplate(req: Uint8Array): Promise<Uint8Array>;
  abstract deleteTemplate(req: Uint8Array): Promise<Uint8Array>;
  abstract createDocumentFromTemplate(req: Uint8Array): Promise<Uint8Array>;
  abstract createDocumentFromDirectTemplate(req: Uint8Array): Promise<Uint8Array>;
  abstract createTemplateDirectLink(req: Uint8Array): Promise<Uint8Array>;
  abstract deleteTemplateDirectLink(req: Uint8Array): Promise<Uint8Array>;
  abstract toggleTemplateDirectLink(req: Uint8Array): Promise<Uint8Array>;
  abstract uploadBulkSend(req: Uint8Array): Promise<Uint8Array>;

  /** Decode a Call envelope, route by method ordinal, and build the Response envelope. */
  async dispatch(envelope: Uint8Array): Promise<Uint8Array> {
    const call: Call = parseRequest(envelope);
    switch (call.method) {
      case TemplateMethod.findTemplates: {
        const body = await this.findTemplates(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case TemplateMethod.getTemplateById: {
        const body = await this.getTemplateById(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case TemplateMethod.getMany: {
        const body = await this.getMany(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case TemplateMethod.createTemplate: {
        const body = await this.createTemplate(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case TemplateMethod.createTemplateTemporary: {
        const body = await this.createTemplateTemporary(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case TemplateMethod.updateTemplate: {
        const body = await this.updateTemplate(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case TemplateMethod.duplicateTemplate: {
        const body = await this.duplicateTemplate(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case TemplateMethod.deleteTemplate: {
        const body = await this.deleteTemplate(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case TemplateMethod.createDocumentFromTemplate: {
        const body = await this.createDocumentFromTemplate(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case TemplateMethod.createDocumentFromDirectTemplate: {
        const body = await this.createDocumentFromDirectTemplate(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case TemplateMethod.createTemplateDirectLink: {
        const body = await this.createTemplateDirectLink(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case TemplateMethod.deleteTemplateDirectLink: {
        const body = await this.deleteTemplateDirectLink(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case TemplateMethod.toggleTemplateDirectLink: {
        const body = await this.toggleTemplateDirectLink(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case TemplateMethod.uploadBulkSend: {
        const body = await this.uploadBulkSend(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      default:
        return buildResponse(Status.NotFound, call.promiseID, new Uint8Array(0));
    }
  }
}


