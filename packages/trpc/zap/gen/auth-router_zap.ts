// Code generated by zapgen --target=ts; DO NOT EDIT.
// source: auth-router.zap

import {
  Builder,
  Message,
  StructView,
  buildRequest,
  parseRequest,
  buildResponse,
  NO_TARGET,
  Status,
  type Call,
  type Response,
} from "@zap-proto/zap";

const reqOff = {
  Body: 0,
} as const;
const reqSize = 8;

/** Zero-copy view into a ZAP-encoded Req message. */
export class Req extends StructView {
  /** Parse `b` and return a typed view. Throws on a wire-level check failure (magic, version, size). */
  static wrap(b: Uint8Array): Req {
    const root = Message.parse(b).root();
    return new Req(root.data, root.offset);
  }

  get body(): string { return this.text(reqOff.Body); }
}

/** Field values for newReq. */
export interface ReqInput {
  body: string;
}

/** Build a ZAP-encoded Req message from `in` and return the bytes. */
export function newReq(in_: ReqInput): Uint8Array {
  const b = new Builder(256);
  const ob = b.startObject(reqSize);
  ob.setText(reqOff.Body, in_.body);
  ob.finishAsRoot();
  return b.finish();
}


const respOff = {
  Body: 0,
} as const;
const respSize = 8;

/** Zero-copy view into a ZAP-encoded Resp message. */
export class Resp extends StructView {
  /** Parse `b` and return a typed view. Throws on a wire-level check failure (magic, version, size). */
  static wrap(b: Uint8Array): Resp {
    const root = Message.parse(b).root();
    return new Resp(root.data, root.offset);
  }

  get body(): string { return this.text(respOff.Body); }
}

/** Field values for newResp. */
export interface RespInput {
  body: string;
}

/** Build a ZAP-encoded Resp message from `in` and return the bytes. */
export function newResp(in_: RespInput): Uint8Array {
  const b = new Builder(256);
  const ob = b.startObject(respSize);
  ob.setText(respOff.Body, in_.body);
  ob.finishAsRoot();
  return b.finish();
}


/** Method ordinals for the Auth service (stable wire ids). */
export const AuthMethod = {
  passkeyCreate: 1,
  passkeyCreateAuthenticationOptions: 2,
  passkeyCreateRegistrationOptions: 3,
  passkeyCreateSigninOptions: 4,
  passkeyDelete: 5,
  passkeyFind: 6,
  passkeyUpdate: 7,
} as const;

/** Typed RPC client for the Auth service over a ZAP call channel. */
export interface AuthChannel {
  /** Ship one Call envelope and await its correlated Response. */
  call(envelope: Uint8Array): Promise<Response>;
}

export class AuthClient {
  constructor(private readonly channel: AuthChannel, private readonly cap: Uint8Array = new Uint8Array(0)) {}

  async passkeyCreate(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: AuthMethod.passkeyCreate,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Auth.passkeyCreate: status ${resp.status}`);
    }
    return resp.body;
  }

  async passkeyCreateAuthenticationOptions(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: AuthMethod.passkeyCreateAuthenticationOptions,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Auth.passkeyCreateAuthenticationOptions: status ${resp.status}`);
    }
    return resp.body;
  }

  async passkeyCreateRegistrationOptions(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: AuthMethod.passkeyCreateRegistrationOptions,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Auth.passkeyCreateRegistrationOptions: status ${resp.status}`);
    }
    return resp.body;
  }

  async passkeyCreateSigninOptions(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: AuthMethod.passkeyCreateSigninOptions,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Auth.passkeyCreateSigninOptions: status ${resp.status}`);
    }
    return resp.body;
  }

  async passkeyDelete(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: AuthMethod.passkeyDelete,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Auth.passkeyDelete: status ${resp.status}`);
    }
    return resp.body;
  }

  async passkeyFind(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: AuthMethod.passkeyFind,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Auth.passkeyFind: status ${resp.status}`);
    }
    return resp.body;
  }

  async passkeyUpdate(req: Uint8Array): Promise<Uint8Array> {
    const envelope = buildRequest({
      method: AuthMethod.passkeyUpdate,
      promiseID: 0,
      target: NO_TARGET,
      cap: this.cap,
      payload: req,
    });
    const resp = await this.channel.call(envelope);
    if (resp.status !== Status.OK) {
      throw new Error(`Auth.passkeyUpdate: status ${resp.status}`);
    }
    return resp.body;
  }

}

/** Abstract RPC server for the Auth service. Subclass and implement each method. */
export abstract class AuthServer {
  abstract passkeyCreate(req: Uint8Array): Promise<Uint8Array>;
  abstract passkeyCreateAuthenticationOptions(req: Uint8Array): Promise<Uint8Array>;
  abstract passkeyCreateRegistrationOptions(req: Uint8Array): Promise<Uint8Array>;
  abstract passkeyCreateSigninOptions(req: Uint8Array): Promise<Uint8Array>;
  abstract passkeyDelete(req: Uint8Array): Promise<Uint8Array>;
  abstract passkeyFind(req: Uint8Array): Promise<Uint8Array>;
  abstract passkeyUpdate(req: Uint8Array): Promise<Uint8Array>;

  /** Decode a Call envelope, route by method ordinal, and build the Response envelope. */
  async dispatch(envelope: Uint8Array): Promise<Uint8Array> {
    const call: Call = parseRequest(envelope);
    switch (call.method) {
      case AuthMethod.passkeyCreate: {
        const body = await this.passkeyCreate(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case AuthMethod.passkeyCreateAuthenticationOptions: {
        const body = await this.passkeyCreateAuthenticationOptions(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case AuthMethod.passkeyCreateRegistrationOptions: {
        const body = await this.passkeyCreateRegistrationOptions(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case AuthMethod.passkeyCreateSigninOptions: {
        const body = await this.passkeyCreateSigninOptions(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case AuthMethod.passkeyDelete: {
        const body = await this.passkeyDelete(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case AuthMethod.passkeyFind: {
        const body = await this.passkeyFind(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      case AuthMethod.passkeyUpdate: {
        const body = await this.passkeyUpdate(call.payload);
        return buildResponse(Status.OK, call.promiseID, body);
      }
      default:
        return buildResponse(Status.NotFound, call.promiseID, new Uint8Array(0));
    }
  }
}


